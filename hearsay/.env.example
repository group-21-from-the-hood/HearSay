VITE_SPOTIFY_CLIENT_ID=
VITE_SPOTIFY_CLIENT_SECRET=
VITE_GOOGLE_CLIENT_ID=
VITE_GOOGLE_CLIENT_SECRET=
VITE_GOOGLE_REDIRECT_URI=http://localhost:5173/auth/callback
VITE_GOOGLE_BACKEND_URL=http://localhost:5174
VITE_MONGO_DB_NAME="hearsay"
VITE_MONGO_URI="mongodb://localhost:27017"
VITE_MONGO_AUTH_SOURCE="admin"

# ===== Backend (Node/Express) env =====
# Mongo connection (either provide MONGO_URI with credentials or username/password fields below)
MONGO_DB_NAME=hearsay
# If you prefer a single URI, uncomment and include credentials + authSource, e.g.:
# MONGO_URI=mongodb://<username>:<password>@localhost:27017/?authSource=admin

# Or supply individual fields (dbAPI will build the URI and pass auth options)
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_PROTOCOL=mongodb
MONGO_AUTH_SOURCE=admin
MONGO_USERNAME=admin  
MONGO_PASSWORD=password

# Session/cookie settings
FRONTEND_ORIGIN=http://localhost:5173
SESSION_NAME=hsid
SESSION_SECRET=replace-with-a-strong-random-secret
SESSION_TTL_SECONDS=3600
SESSION_ROTATE_MS=3600000
SESSIONS_COLLECTION=expressSessions